const initialState = {
  ingredients: [],
  selectedIngredients: [],
  currentIngredient: {},
  order: {}
}

export const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state
  }
}